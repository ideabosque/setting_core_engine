# -*- coding: utf-8 -*-
"""PostgreSQL models package for setting_core_engine.

Models are imported lazily by the repository layer after Config has set
``Base.table_prefix``. Do NOT import models here — doing so binds
``__table__.name`` before the prefix is configured.
"""
from __future__ import print_function

__all__ = []